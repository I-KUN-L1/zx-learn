=====================================================================
知行智学 ZhiXing Learn · 课程界面 / 我的课表 / 章节内容 优化 验证报告
生成时间: 2026-09-18 19:07:33
网关地址: http://localhost:8080
=====================================================================

=== 一、环境与构建产物 ===
  [PASS] zx-course fat jar 存在
  [PASS] zx-learning fat jar 存在
  [PASS] zx-trade fat jar 存在
  [PASS] zx-gateway fat jar 存在
  [PASS] 前端构建产物存在
  [PASS] course_catalogue 四个内容列已就位

=== 二、服务就绪检查（复用模式）===
  [PASS] zx-course 已在运行 (8083)
  [PASS] zx-learning 已在运行 (8086)
  [PASS] zx-trade 已在运行 (8087)
  [PASS] zx-gateway 已在运行 (8080)

=== 三、章节内容模块：目录与讲义补全（需求 2）===
  [INFO] 在架课程数：13（口径：deleted=0 且 status=1）
  [PASS] 逐门课程自检覆盖全部在架课程（13 门，防静默跳过）
  [PASS] 全部课程详情接口可解析
  [PASS] 每门课程都至少有一个章节目录节点
  [PASS] 每门课程都至少有一个小节
  [PASS] 所有小节都有讲义正文（无空讲义）
  [PASS] 所有小节都有本节要点
  [PASS] 学员登录 13900000001/123456
  [PASS] 课程详情 3001 返回 200
  [PASS] 3001 章节数 ≥ 2
  [PASS] 3001 小节数 ≥ 4
  [PASS] 讲义为真实内容（含“本节目标”而非占位文案）
  [PASS] 要点已按 “|” 下发（可切分）
  [PASS] 库内无空讲义小节（SQL 交叉校验）
  [PASS] 每门在架课程目录结构 ≥ 2 章 × 4 小节（共 13 门）
  [PASS] 目录节点名无英文占位（均为中文教学内容）
  [PASS] 无在架课程出现空目录（13 门均已配置章节）

=== 四、课程界面 ↔ 我的课表：「已拥有」口径与实时联动（需求 1）===
  [PASS] GET /lessons/mine/course-ids 返回 200
  [PASS] 课表课程 id 为数组
  [PASS] 接口返回的已拥有集合与 lesson 表完全一致
  [PASS] 前置：课程 3006 不在学员课表中但已有已支付订单
  [PASS] 旧口径 /orders/bought-course-ids 含该课程（课表∪已支付，会误显示已拥有）
  [PASS] 新口径 /lessons/mine/course-ids 不含该课程（以课表为准 → 显示可购买）
  [PASS] 内部开课接口返回 HTTP 200
  [PASS] 同步开课后立即出现在课表（已拥有）
  [PASS] 两端一致：该课程同时出现在「我的课表」列表中
  [PASS] 外部用户经网关调用内部开课接口被拒
  [PASS] 场景收尾：已支付课程已补开课（不留「已支付但课表缺失」脏状态）
  [PASS] 历史已支付订单未被破坏（保留基线数据）

=== 五、支付成功 → 课表立即开通 → 两端一致（需求 1 的实时性）===
  [PASS] 合成课程已入库（2 门）
  [PASS] 开课前：付费课程不在课表
  [PASS] 下单成功
  [PASS] 取到订单 id=756108973861601280
  [PASS] Mock 支付成功
  [PASS] 订单状态已支付（库）
  [PASS] 支付返回后【立即】出现在课表（同步开课，不依赖 MQ）
  [PASS] 「我的课表」列表【立即】出现该课程（两端一致）
  [PASS] 免费课 0 元开课成功
  [PASS] 免费开课后【立即】出现在课表
  [PASS] 「我的课表」列表【立即】出现免费课（两端一致）
  [PASS] 已拥有后再次加入学习：幂等成功（不再报「课程已拥有」）
  [PASS] 幂等：重复加入未产生重复课表项

=== 六、章节内容子模块：按小节归档的笔记 ===
  [PASS] 新增「本节笔记」成功
  [PASS] 按 课程+小节 精确过滤命中 1 条
  [PASS] 笔记返回体含 lessonId
  [PASS] 不同小节互不串数据（过滤生效）
  [PASS] 「我的笔记」总列表仍能看到该笔记
  [PASS] 删除本节笔记成功
  [PASS] 删除后本节笔记为空

=== 七、前端产物校验 ===
  [PASS] 我的课表：课程检索模块已构建
  [PASS] 检索结果按课表判定持有状态（已拥有/可购买）
  [PASS] 章节内容：本节笔记模块已构建
  [PASS] 章节内容：本节要点模块已构建
  [PASS] 封面兜底组件已构建（消除裂图空白占位）
  [PASS] 章节内容：我的学习记录模块已构建
  [PASS] 未拥有课程展示可购买状态
  [PASS] 已拥有状态由 useOwnedCourses 统一提供（三端共用）
  [PASS] 课程列表不再直接依赖订单口径 boughtCourseIds
  [PASS] 课程详情不再直接依赖订单口径 boughtCourseIds
  [PASS] 合成课程已清理
  [PASS] 合成课表项已清理
  [PASS] 合成订单已清理
  [PASS] 合成笔记已清理

=== 八、汇总 ===
  断言总数: 72   通过: 72   失败: 0
  结论: 全部通过 ✅

---------------------------------------------------------------------
逐门课程目录自检: logs/verify-course-ui/catalogue-summary.txt
原始响应报文目录: logs/verify-course-ui/
服务启动日志目录: logs/verify-course-ui/svc-*.log
---------------------------------------------------------------------
