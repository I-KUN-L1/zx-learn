=====================================================================
知行智学 · 优惠券使用状态同步 端到端验证报告
生成时间: 2026-09-18 18:46:20
网关地址: http://localhost:8080
=====================================================================

=== 0. 准备：合成学员 + 优惠券 + 领取 ===
  [PASS] 预检：网关(8080)可达
  [PASS] 预检：营销服务(8088)已启动（需 EXTRA_SPECS="zx-promotion:8088"）
  [PASS] 合成学员登录成功
  [PASS] 领取优惠券成功
  [PASS] 前置：券已领取且状态为「未使用」
  [INFO] 合成用户券行 id = 2100899034427359233

=== 1. 下单用券 → 券状态实时同步为「已使用」（核心需求）===
  [PASS] 用券下单成功
  [INFO] 合成订单 id = 756103573187674112
  [PASS] 【核心】券状态立即更新为「已使用」(status=1)
  [PASS] 【核心】回填核销订单号
  [PASS] 【核心】回填使用时间
  [INFO] 核销流水条数（MQ 通道，异步）：1

=== 2. 优惠券列表接口同步返回「已使用」===
  [PASS] 券列表接口返回该券为「已使用」(前端 status=2)
  [PASS] 按「已使用」筛选能查到该券
  [PASS] 按「未使用」筛选已查不到该券（券界面不再显示可用）
  [PASS] 该学员「未使用」券数量归零（券界面可用数同步下降）

=== 3. 重复使用同一张券：拦截 + 提示语准确（原症状）===
  [PASS] 重复使用同一张券被拒（不产生第二笔订单）
  [INFO] 拦截提示语：该优惠券已使用过（每张券限用一次），请选择其它优惠券
  [PASS] 提示语说明真实原因（已使用过），不再误报「已达领取上限」
  [PASS] 被拒后未产生订单
  [PASS] 被拒后券状态未被打乱（仍为已使用）

=== 4. 关单退券：状态退回「未使用」，券恢复可用 ===
  [PASS] 待支付订单关单成功
  [PASS] 订单状态已关闭
  [PASS] 【核心】关单后券状态退回「未使用」(status=0)
  [PASS] 关单后清空核销订单号（不再被已关闭订单占住）
  [PASS] 关单后清空使用时间
  [PASS] 券列表重新显示为「未使用」（券界面恢复可用）
  [PASS] 退回后券可再次使用（券状态与限用计数同时恢复）
  [PASS] 再次使用后券状态又变为「已使用」
  [PASS] 券绑定到新订单

=== 5. 回写幂等：重复调用不产生副作用 ===
  [PASS] 重复回写「已使用」返回 HTTP 200（幂等，不报错）
  [PASS] 重复回写不覆盖已有绑定（仍是当前订单）
  [PASS] 重复回写后状态仍为「已使用」
  [PASS] 退回一个不存在的订单号返回 HTTP 200（幂等空操作）
  [PASS] 无关订单的退回未影响该券状态
  [PASS] 无关订单的退回未影响绑定订单
  [PASS] 回写一张不存在的券返回 HTTP 200（容错，不抛异常）

=== 6. 内部回写接口鉴权：外部不可调用 ===
  [PASS] 服务间直连调用内部回写接口放行（HTTP 200）
  [PASS] 学员经网关调用内部回写接口被拒 403
  [PASS] 匿名经网关调用内部回写接口被网关拒绝（HTTP 401）

=== 7. 全库存量一致性 ===
  [PASS] 全库「券状态与最后一条核销流水」漂移数 = 0
  [PASS] 全库「有核销流水但未回填订单号」的券 = 0
  [INFO] 种子数据中本就标记为已使用、无订单号的券数量（非本模块产生，仅记录）：5
  [PASS] 状态回写接口未产生越权副作用（合成券仍是已使用）

=== 8. 前端：券中心与下单页共用同一份状态 ===
  [PASS] 券状态全局单例 composable 已创建
  [PASS] 单例提供乐观更新 markUsed（用券后立即置为已使用）
  [PASS] 单例提供「可用券」统一判定 usableCoupons
  [PASS] 优惠券中心改为读取全局单例
  [PASS] 优惠券中心不再各自拉取一份（避免两份状态不一致）
  [PASS] 确认下单页改为读取同一份可用券
  [PASS] 下单成功后立即标记该券为已使用（前端即时呈现）
  [PASS] 前端构建产物存在
  [PASS] 构建产物包含优惠券中心分包

=== 9. 清理夹具 ===
  [PASS] 合成用户券已清理
  [PASS] 合成券模板已清理
  [PASS] 合成订单已清理
  [PASS] 合成核销流水已清理
  [PASS] 合成课程已清理
  [PASS] 合成学员已清理
  [PASS] 清理后全库漂移仍为 0（未破坏真实数据）

---------------------------------------------------------------------
断言总数: 55   通过: 55   失败: 0
原始报文目录: logs/verify-coupon-status/
---------------------------------------------------------------------
