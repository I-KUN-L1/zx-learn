=====================================================================
知行智学 · 课表一致性（已拥有 ↔ 我的课表）端到端验证报告
生成时间: 2026-09-18 18:56:14
网关地址: http://localhost:8080
=====================================================================
  [PASS] 合成学员登录成功

=== 一、付费课：已支付但课表缺失 → 下单自愈补开（症状 1）===
  [PASS] 前置：已支付订单存在、课表为空（脏状态已构造）
  [PASS] 下单被拦截（已支付订单，不再重复建单）
  [INFO] 拦截提示语：您已购买该课程，已为你开通学习，可直接开始学习
  [PASS] 拦截提示明确告知已开通学习（不再只说'已拥有'）
  [PASS] 【核心】自愈：课表已补开该课程
  [PASS] 自愈后「已拥有」集合含该课程（两端一致）
  [PASS] 自愈后「我的课表」列表可见该课程

=== 二、免费课：已支付但课表缺失 → 加入学习幂等成功（症状 2）===
  [PASS] 前置：免费课已支付订单存在、课表为空
  [PASS] 【核心】加入学习返回成功（不再报课程已拥有）
  [PASS] 【核心】自愈：课表已补开该免费课
  [PASS] 自愈后「已拥有」集合含该免费课

=== 三、已开通后重复加入学习：幂等成功 ===
  [PASS] 重复加入学习仍返回成功（幂等，不报错）
  [PASS] 幂等：课表未产生重复项

=== 四、课表项被删除后重新开课：必须复活（不永久隐形）===
  [PASS] 前置：删除后该课程不再属于已拥有
  [PASS] 重新开课后课表项复活（deleted=0）
  [PASS] 复活后可再次「已拥有」

=== 五、orderPaid 死信 → 对账补偿补开课 ===
  [PASS] 前置：课表为空 + orderPaid 处于死信
  [PASS] 对账补偿本轮补开课 ≥ 1 门
  [PASS] 补偿后课表已补开该课程
  [PASS] 补偿后死信被标记为已消费（不再重复补偿）
  [PASS] 再次对账补开课为 0（幂等）

=== 六、存量数据：已支付订单必须都能在课表中找到 ===
  [PASS] 全库「已支付但课表缺失」= 0
  [PASS] 全库无 orderPaid 死信残留
  [PASS] 全库无被逻辑删除的课表项（与已支付订单冲突）

=== 七、前端产物：课表布局（需求 3）===
  [PASS] 课表每页 9 门（3 列 × 3 行，不再出现第 9 门被挤到下一页）
  [PASS] 源码已移除旧的每页 8 门设置
  [PASS] 小规模课表整屏展示（不超过 15 门不分页）
  [PASS] 前端构建产物存在
  [PASS] 构建产物包含新的分页提示文案（共 N 门/本页显示）
  [PASS] 合成学员已清理
  [PASS] 合成课程已清理
  [PASS] 合成订单已清理
  [PASS] 合成课表已清理
  [PASS] 合成消息已清理

---------------------------------------------------------------------
断言总数: 34   通过: 34   失败: 0
原始报文目录: logs/verify-lesson-consistency/
---------------------------------------------------------------------
