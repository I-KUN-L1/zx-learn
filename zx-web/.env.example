# 环境变量说明（复制为 .env.local 或修改对应 .env.* 文件）
# Axios 基地址：开发环境经 Vite proxy 代理到 VITE_PROXY_TARGET，生产环境经反向代理转发到网关
VITE_API_BASE_URL=/api
# Vite 开发代理目标（网关地址）
VITE_PROXY_TARGET=http://localhost:8080
# Mock 开关：true 时无需后端即可演示全部页面（含模拟 SSE 流式输出）
VITE_USE_MOCK=false
